# =====================================================================
#  Dependencies — Judo sub-technique classification (MC3-18, RGB-only)
# =====================================================================
#
#  PyTorch: the versions below assume a CUDA 12.1 GPU. If torch does not see
#  the GPU, reinstall following the official command for your CUDA version:
#      https://pytorch.org/get-started/locally/
#  E.g. (CUDA 12.1):
#      pip install torch==2.2.2 torchvision==0.17.2 \
#          --index-url https://download.pytorch.org/whl/cu121
#
#  These versions form a tested-compatible set.
# =====================================================================

torch==2.2.2
torchvision==0.17.2

numpy==1.26.4
pandas==2.2.2
scikit-learn==1.4.2      # StratifiedGroupKFold (source-wise split)
opencv-python==4.9.0.80
matplotlib==3.8.4
Pillow==10.3.0
scipy==1.13.0
tqdm==4.66.4

# video reading (main.py uses decord if available, otherwise falls back to OpenCV)
av==12.0.0
decord==0.6.0

# auxiliary YOLO (optional; only used with --spatial_mode yolo_union_crop or
# --input_modality two_stream, for the two-stream experiments discussed in the paper).
# The released RGB-only model uses --spatial_mode full_frame and does NOT need this.
ultralytics==8.2.0
